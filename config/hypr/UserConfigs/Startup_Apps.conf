# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Commands and Apps to be executed at launch

$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts

$wallDIR=$HOME/Pictures/wallpapers
$lock = $scriptsDir/LockScreen.sh
$SwwwRandom = $UserScripts/WallpaperAutoChange.sh
$livewallpaper=""

# wallpaper stuff 
exec-once = swww-daemon --format xrgb
#exec-once = mpvpaper '*' -o "load-scripts=no no-audio --loop" $livewallpaper

# wallpaper random
#exec-once = $SwwwRandom $wallDIR # random wallpaper switcher every 30 minutes

# Startup
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

# Polkit (Polkit Gnome / KDE)
exec-once = $scriptsDir/Polkit.sh

# starup apps
exec-once = nm-applet --indicator
exec-once = swaync
#exec-once = ags
#exec-once = blueman-applet
#exec-once = rog-control-center
exec-once = waybar
exec-once = qs # quickshell AGS Desktop Overview alternative

#clipboard manager
exec-once = wl-paste --type text --watch cliphist store
exec-once = wl-paste --type image --watch cliphist store

# Rainbow borders
exec-once = $UserScripts/RainbowBorders.sh

# Starting hypridle to start hyprlock
exec-once = hypridle


# Here are list of features available but disabled by default
# exec-once = swww-daemon --format xrgb && swww img $HOME/Pictures/wallpapers/mecha-nostalgia.png  # persistent wallpaper

#gnome polkit for nixos
#exec-once = $scriptsDir/Polkit-NixOS.sh

# xdg-desktop-portal-hyprland (should be auto starting. However, you can force to start)
#exec-once = $scriptsDir/PortalHyprland.sh











#===========================================================================================================#
#                                                  BOTTOMBAR                                                #
#===========================================================================================================#
#Here we have a waybar watchdog block.
#Essentially it launches a watchdog script for each declarated
#in here instance of the waybar.
#Example:
# Bottom bar
#------------This is a default location-------------#
#exec-once = ~/.config/waybar/bottombar/waybar-hover-toggle.sh
#~/.config/hypr/UserScipts/waybar-watchdog.sh \
#  "$HOME/.config/waybar/bottombar/config-bottom.jsonc" \
#  "$HOME/.config/waybar/bottombar/style-custom-bottom-bar.css" &
#Uncomment three lines above to start using this feature.
#Obviously, it makes literally no sense if you already have some bar bositioned on the bottom of the screen,
#however if you have only top bar - you can find this useful.
#[TOP] 0-Ja-0 + this bottombar was my default setup for a few months since now and I find it to be perfect)
#It is essential to execute the bar this way or manually, otherwise because of the wallpaper switch
#it can either be killed because of the CSS being changed (at tis point CSS imports some colors from the wallust)
#Or, if the hower script is not initialised - the bar can start flicker, so I strongly recommend to execute it this way.
